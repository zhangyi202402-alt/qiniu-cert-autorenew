# 复制为 .env 并填写真实值；.env 勿提交 Git。
# 使用: source .env  或运行 scripts 时会自动加载（若存在 .env）

# 七牛 API
QINIU_AK=你的七牛AccessKey
QINIU_SK=你的七牛SecretKey

# 阿里云 DNS（acme.sh dns_ali 要求的环境变量名固定为 Ali_Key / Ali_Secret）
# RAM 子账号需 AliyunDNSFullAccess 权限
Ali_Key=你的阿里云AccessKeyId
Ali_Secret=你的阿里云AccessKeySecret

# 钉钉告警（可选）
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=xxx

# 可选：Docker Compose 挂载的配置（默认 config.docker.yaml）
# QINIU_CERT_CONFIG_FILE=./config.yaml
